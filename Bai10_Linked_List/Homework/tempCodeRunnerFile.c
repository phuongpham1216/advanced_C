 // pop_back(&arr);

    // indexNode = size(&arr);
    // printf("so node cua list = %d\n", indexNode);

    // for(i=0; i<indexNode; i++){
    //     printf("Value test: %d\n", get(arr, i));
    // }

    // push_front(&arr, 50);

    // indexNode = size(&arr);
    // printf("so node cua list = %d\n", indexNode);

    // for(i=0; i<indexNode; i++){
    //     printf("Value test: %d\n", get(arr, i));
    // }